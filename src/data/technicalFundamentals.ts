import type { InterviewCard } from '@/types/interview'

type Flashcard = [
  id: string,
  question: string,
  answer: string,
  beats: string[],
  followUp: string,
  code?: string,
]

const flashcards: Flashcard[] = [
  [
    'event-loop-order',
    'What is the basic JavaScript event-loop execution order?',
    'Run synchronous code, drain all microtasks, then run the next task. Drain microtasks again after that task and repeat.',
    [
      'Synchronous call stack first',
      'Drain microtasks before the next task',
      'Repeat after each task',
    ],
    'What changes if a microtask queues another microtask?',
  ],
  [
    'common-microtasks',
    'What are common microtasks?',
    'Promise then, catch, and finally callbacks; continuation after await; and queueMicrotask. They run after current synchronous code and before the next task.',
    ['Promise callbacks', 'Continuation after await', 'queueMicrotask runs before the next task'],
    'Can a microtask queue another microtask?',
  ],
  [
    'common-tasks',
    'What are common tasks/macrotasks?',
    'Timers, such as setTimeout and setInterval, and browser events such as clicks are common tasks. Their callbacks run on a future event-loop turn.',
    ['Timers', 'Browser event callbacks', 'Future event-loop turn'],
    'Where do microtasks run relative to two tasks?',
  ],
  [
    'event-loop-output',
    'What prints?',
    'A, D, C, B. Synchronous logs run first, then the Promise microtask, then the timer task.',
    ['A then D synchronously', 'C from Promise microtask', 'B from timer task'],
    'What if the Promise callback schedules another Promise callback?',
    'console.log("A")\nsetTimeout(() => console.log("B"), 0)\nPromise.resolve().then(() => console.log("C"))\nconsole.log("D")',
  ],
  [
    'timeout-zero',
    'Does setTimeout(fn, 0) execute immediately?',
    'No. It becomes eligible after the minimum delay and runs only when the event loop processes that task.',
    ['Not immediate', 'Minimum delay', 'Waits for the event loop'],
    'What runs first: a resolved Promise callback or a zero-delay timer?',
  ],
  [
    'async-thread',
    'Does declaring a function async move its work onto another thread?',
    'No. It changes Promise behavior; CPU-intensive JavaScript can still block the main thread.',
    ['No new thread', 'Async works with Promises', 'CPU work can still block'],
    'What would you use to move CPU work off the main thread?',
  ],
  [
    'cpu-vs-io',
    'What is the difference between CPU-bound and I/O-bound work?',
    'CPU-bound work spends time calculating and may need a worker, chunking, or a backend. I/O-bound work spends time waiting on a network, database, or service and benefits from asynchronous concurrency.',
    ['CPU work consumes processing time', 'I/O work waits externally', 'Different remedies'],
    'Which category is fetching transaction records?',
  ],
  [
    'concurrency-parallelism',
    'What is concurrency vs parallelism?',
    'Concurrency lets operations make progress during waits. Parallelism means work physically executes simultaneously. Async/await supports concurrency but does not automatically parallelize CPU work.',
    [
      'Concurrency overlaps progress',
      'Parallelism executes simultaneously',
      'Async is not CPU parallelism',
    ],
    'Can two fetches be concurrent on one JavaScript thread?',
  ],
  [
    'sequential-fetch',
    'Why is this slow?',
    'Each independent request waits for the previous one. Use concurrent or bounded requests, batching, or an API redesign where appropriate.',
    [
      'Await inside the loop serializes requests',
      'Requests are independent',
      'Concurrency or batching can reduce latency',
    ],
    'When would bounded concurrency be safer than Promise.all?',
    'for (const id of ids) {\n  const response = await fetch(`/transactions/${id}`)\n}',
  ],
  [
    'promise-all',
    'What does Promise.all() do?',
    'It waits for multiple Promises concurrently and resolves with their values when all succeed. It rejects if any one rejects.',
    ['Concurrent waiting', 'All must fulfill', 'Any rejection rejects the aggregate'],
    'Does rejection cancel the other operations?',
  ],
  [
    'promise-all-thousands',
    "Why shouldn't you always use Promise.all() on thousands of requests?",
    'Unbounded requests can overwhelm a backend, hit rate limits, exhaust client resources, and create traffic spikes. Use batching or bounded concurrency.',
    ['Unbounded load', 'Rate or resource limits', 'Batch or bound concurrency'],
    'How would you choose a concurrency limit?',
  ],
  [
    'all-vs-all-settled',
    'Promise.all() vs Promise.allSettled()?',
    'Promise.all rejects when any input rejects. Promise.allSettled waits for every input and returns each fulfillment or rejection.',
    ['all fails on one rejection', 'allSettled waits for all', 'allSettled reports each outcome'],
    'Which would you use for independent optional widgets?',
  ],
  [
    'closure-definition',
    'What is a closure?',
    'A function that retains access to variables from the lexical scope where it was created, even after the outer function has returned.',
    [
      'Function retains lexical scope',
      'Variables remain accessible',
      'Outer function may have returned',
    ],
    'What is one practical use for a closure?',
  ],
  [
    'closure-counter',
    'What does this print?',
    '1, 2, 1, 3. Each createCounter invocation creates a separate lexical environment.',
    ['a increments independently', 'b starts at one', 'Output is 1, 2, 1, 3'],
    'What would happen if both functions shared one outer count?',
    'function createCounter() {\n  let count = 0\n  return () => ++count\n}\nconst a = createCounter()\nconst b = createCounter()\nconsole.log(a())\nconsole.log(a())\nconsole.log(b())\nconsole.log(a())',
  ],
  [
    'closure-survival',
    'Why does count survive after createCounter() returns?',
    'The returned function closes over count in its lexical environment. It is not because count was declared with let.',
    [
      'Returned function references count',
      'Lexical environment is retained',
      'Not a special property of let',
    ],
    'What happens when no function references that environment anymore?',
  ],
  [
    'vue-reactivity',
    'What does Vue reactivity mean?',
    'Vue tracks dependencies between reactive state and component rendering. When that state changes, Vue schedules affected updates and applies the necessary DOM changes.',
    ['Tracks state dependencies', 'Schedules affected updates', 'Updates the DOM'],
    'What happens if you mutate a nonreactive local variable?',
  ],
  [
    'vue-ref',
    'What does ref() do?',
    'It creates reactive Vue state. In JavaScript, access the value through .value; templates generally unwrap refs.',
    ['Creates reactive state', 'Use .value in JavaScript', 'Templates unwrap refs'],
    'How do you update a ref containing an array?',
    'const transactions = ref([])\ntransactions.value',
  ],
  [
    'ref-vs-reactive',
    'ref() vs reactive()?',
    'ref creates a reactive container around a value, including primitives or objects. reactive creates a reactive proxy around an object.',
    ['ref wraps a value', 'reactive proxies an object', 'ref works with primitives'],
    'Why might you favor ref for a replaceable object?',
    'const count = ref(0)\nconst user = reactive({ name: "Oscar" })',
  ],
  [
    'computed-use',
    'When should you use computed()?',
    'Use computed for a value derived from reactive state, such as a filtered list of pending transactions.',
    ['Derived value', 'Depends on reactive state', 'Not a side effect'],
    'What happens to a computed value when its dependencies change?',
    'const pending = computed(() =>\n  transactions.value.filter(t => t.status === "pending")\n)',
  ],
  [
    'watch-use',
    'When should you use watch()?',
    'Use watch when reactive changes should trigger a side effect, such as analytics, logging, an API call, or synchronization.',
    ['Reactive change is observed', 'Trigger a side effect', 'Not merely derived state'],
    'When would computed be better than watch?',
  ],
  [
    'pending-computed',
    'Pending transactions: computed or watch?',
    'Computed, because pending transactions are a value derived from the transaction list.',
    ['Choose computed', 'Derived list', 'No side effect'],
    'What would make a watch appropriate here?',
  ],
  [
    'analytics-watch',
    'Send analytics when pending count changes: computed or watch?',
    'Watch, because sending analytics is a side effect caused by a reactive change.',
    ['Choose watch', 'Analytics is a side effect', 'Responds to a change'],
    'How would you avoid sending duplicate analytics events?',
  ],
  [
    'vue-props',
    'What are props?',
    'Props are explicit inputs from a parent to a child component. They make dependencies visible and support testing, reuse, and predictable data flow.',
    ['Parent-to-child inputs', 'Explicit dependencies', 'Predictable reusable components'],
    'How should a child request a parent-owned state change?',
  ],
  [
    'prop-drilling',
    'What is prop drilling?',
    'Passing data through intermediate components that do not use it solely to reach a deeply nested child.',
    [
      'Intermediate components pass data',
      'They do not need it themselves',
      'Deep descendant is the consumer',
    ],
    'When would you tolerate prop drilling?',
  ],
  [
    'shared-state',
    'When should state become shared/global?',
    'When sufficiently distant parts of an application genuinely need the same state. Keep state in the narrowest scope that reasonably owns it; do not globalize it merely to avoid props.',
    [
      'Genuine broad sharing',
      'Narrowest reasonable owner',
      'Avoid globalizing just to avoid props',
    ],
    'What would you do if only a parent and child need the state?',
  ],
  [
    'server-vs-ui-state',
    'Server state vs UI/client state?',
    'Transactions, balances, payment status, and merchant data are server state. Selected tabs, open modals, filters, and sort order are client UI state. Server state often belongs in a query/cache layer.',
    [
      'Distinguish server-owned data',
      'Distinguish local UI choices',
      'Use a query/cache layer where useful',
    ],
    'Which source is authoritative for a displayed balance?',
  ],
  [
    'main-thread-freeze',
    'Why can CPU-heavy JavaScript freeze the UI?',
    'JavaScript, rendering, and interactions commonly share the browser main thread. Long-running JavaScript prevents input handling and rendering frames.',
    ['Shared main thread', 'Long task blocks interaction', 'Rendering frames are delayed'],
    'How would you confirm a long task is the cause?',
  ],
  [
    'web-worker-use',
    'When would you use a Web Worker?',
    'For CPU-intensive calculations or processing that would otherwise block the main UI thread, such as large datasets, compression, or image work.',
    ['CPU-intensive work', 'Avoid blocking UI', 'Examples such as processing or compression'],
    'What data-transfer costs should you consider?',
  ],
  [
    'worker-fetch',
    'Would you use a Web Worker for fetch() calls?',
    'Usually not. Fetch is I/O-bound and already asynchronous, so a worker typically adds no benefit for the waiting itself.',
    ['Usually no', 'Network is I/O-bound', 'Fetch is already asynchronous'],
    'When might processing a fetched payload justify a worker?',
  ],
  [
    'slow-page-first-step',
    'What is the first step when a user says the page is slow?',
    'Measure what is actually slow: network, processing, rendering, assets, rerenders, or perceived latency. Avoid optimizing from intuition alone.',
    ['Measure first', 'Locate the bottleneck', 'Consider perceived performance'],
    'Which tool would you use to inspect main-thread work?',
  ],
  [
    'perceived-performance',
    'How can you improve perceived performance?',
    'Show useful content sooner with cached data, skeletons, progressive loading, pagination, lazy images, or prefetching. The system can feel faster without every operation completing faster.',
    [
      'Show useful content sooner',
      'Use progressive or cached content',
      'Distinguish perceived from actual latency',
    ],
    'When can a skeleton make an experience worse?',
  ],
  [
    'paginate-transactions',
    'Why paginate transaction history?',
    'Users rarely need their entire history at once. Pagination reduces payload size, server work, client processing, and rendering.',
    [
      'Only fetch needed history',
      'Smaller payload and server work',
      'Less client processing and rendering',
    ],
    'How would you keep page boundaries stable as new transactions arrive?',
  ],
  [
    'stale-while-revalidate',
    'What is stale-while-revalidate?',
    'Show cached data immediately, fetch fresh data in the background, update the cache, and update the UI if the data changed.',
    ['Show cached data', 'Fetch fresh data in background', 'Update cache and UI'],
    'Which financial data might be unsafe to show stale?',
  ],
  [
    'financial-cache-risk',
    "What's dangerous about caching financial data?",
    'Different financial data needs different freshness. History may tolerate brief staleness, while balances and active payment status may require stronger guarantees.',
    [
      'Freshness varies by data type',
      'History may tolerate staleness',
      'Balances and payments need care',
    ],
    'How would you signal stale data to a customer?',
  ],
  [
    'financial-source-truth',
    'Who is the source of truth for financial data?',
    'Usually the authoritative backend or service. A frontend cache is a shared representation, not the ultimate authority.',
    [
      'Authoritative backend/service',
      'Frontend cache is a representation',
      'Avoid treating cache as final truth',
    ],
    'How would the UI reconcile after a payment timeout?',
  ],
  [
    'bff-definition',
    "What's a BFF?",
    'A Backend For Frontend is a backend layer designed around one client’s needs. It can sit between the UI and backend services and provide client-friendly APIs.',
    ['Backend For Frontend', 'Client-oriented layer', 'Sits between UI and services'],
    'What responsibility would you keep out of a BFF?',
  ],
  [
    'bff-benefits',
    'Why might a BFF be useful?',
    'It can hide backend topology, aggregate services, shape client-specific responses, centralize orchestration or authorization, and reduce frontend coupling.',
    ['Hide and aggregate services', 'Client-specific response model', 'Reduce coupling'],
    'What could make a BFF an unnecessary layer?',
  ],
  [
    'api-design-question',
    'Before designing an API endpoint, what should you ask?',
    'Ask what the user is trying to accomplish and exactly what information and actions the UI needs. Start with the use case.',
    ['User goal first', 'UI information and actions', 'Then design endpoints'],
    'How would this affect an API for transaction details?',
  ],
  [
    'batch-api',
    'When might one API call be better than 100?',
    'When a client needs many related resources, a batch endpoint can avoid a request per item and reduce latency and overhead.',
    ['Many related resources', 'Batch instead of one request per item', 'Reduce network overhead'],
    'What limit would you put on a batch endpoint?',
    'GET /transactions?ids=...\n// instead of GET /transactions/1, /2, ...',
  ],
  [
    'blind-payment-retry',
    'Why is blindly retrying a payment dangerous?',
    'The first request may have succeeded even if the client did not receive its response. A blind retry can perform the operation twice.',
    [
      'Lost response does not mean failure',
      'First operation may have succeeded',
      'Duplicate payment risk',
    ],
    'How would an idempotency key help?',
  ],
  [
    'idempotency',
    'What is idempotency?',
    'Repeating an operation has the same intended effect as performing it once. It is crucial for safely retrying payment mutations.',
    ['Same intended effect on repetition', 'Supports safe retries', 'Important for payments'],
    'Where should an idempotency key be enforced?',
  ],
  [
    'payment-timeout-state',
    'What is an important distinction when a payment request times out?',
    'A timeout does not prove the payment failed. The outcome may be unknown or pending and must be reconciled with the authoritative service.',
    ['Timeout is not definite failure', 'Represent unknown/pending', 'Reconcile with authority'],
    'What should the customer see while reconciliation is in progress?',
  ],
  [
    'payment-failure-experience',
    'What is a useful payment principle?',
    'The failure experience is part of the product. Show a meaningful customer state rather than leaving them with nothing happening.',
    [
      'Failure is part of product design',
      'Meaningful customer state',
      'Do not leave ambiguity invisible',
    ],
    'How would you word an unknown outcome without falsely claiming failure?',
  ],
  [
    'pay-does-nothing',
    'First question for clicking Pay does nothing?',
    'Define does nothing and find where the sequence stops: click, handler, validation, request, backend, response, state update, or UI.',
    ['Clarify observed symptom', 'Trace the full path', 'Find where it stops'],
    'What would you inspect first if no request appears in DevTools?',
  ],
  [
    'http-request-split',
    'Why check whether an HTTP request was generated?',
    'It divides the failure domain. No request suggests frontend event, state, or validation issues; an existing request points toward network, backend, or response handling.',
    [
      'Quick failure-domain split',
      'No request suggests frontend',
      'Request exists points downstream',
    ],
    'Could a service worker complicate this split?',
  ],
  [
    'unobserved-errors',
    'No error occurred vs we did not observe an error?',
    'They are not equivalent. Intermittent production problems need logs, telemetry, error reporting, metrics, traces, and correlation IDs to reveal hidden failures.',
    [
      'Absence of evidence differs from no error',
      'Need observability',
      'Correlation IDs connect events',
    ],
    'Which signal would you add first for an intermittent payment issue?',
  ],
  [
    'intermittent-bug',
    'How do you investigate an intermittent production-only bug?',
    'Look for correlations by browser, OS, version, network, geography, account state, payment type, timing, and session state. Apparently random failures often share a condition.',
    ['Segment affected cases', 'Compare environment and timing', 'Find hidden common condition'],
    'How would you investigate without exposing sensitive payment data?',
  ],
  [
    'testing-layers',
    'What testing layers should you consider?',
    'Use unit tests for rules, integration tests for service boundaries and persistence, component tests for UI behavior, and end-to-end tests for critical journeys.',
    ['Unit for logic', 'Integration and component boundaries', 'E2E for critical journeys'],
    'Where would you test payment idempotency?',
  ],
  [
    'payment-test-edges',
    'What payment edge cases should you test?',
    'Test double clicks, timeouts, network loss, retries, downstream success with client timeout, app close or refresh, pending-to-completed transitions, duplicate requests, and expired authentication.',
    [
      'Duplicate submission and retry',
      'Ambiguous timeout and network loss',
      'Recovery and state transitions',
    ],
    'How would you make a timeout-after-success test deterministic?',
  ],
  [
    'mental-cpu',
    'CPU problem?',
    'Move work to a Web Worker, chunk it, or process it on the backend.',
    ['Web Worker', 'Chunking', 'Backend processing'],
    'How would you decide which remedy to use?',
  ],
  [
    'mental-io',
    'I/O problem?',
    'Use asynchronous work and appropriate concurrency.',
    ['Async operations', 'Concurrency', 'Avoid blocking on independent waits'],
    'When should concurrency be bounded?',
  ],
  [
    'mental-many-requests',
    'Too many network requests?',
    'Use bounded concurrency, batching, or redesign the API.',
    ['Bound concurrency', 'Batch related work', 'Consider API redesign'],
    'When would batching be preferable to concurrent calls?',
  ],
  [
    'mental-derived-state',
    'Derived Vue state?',
    'Use computed for a value derived from reactive state.',
    ['Use computed', 'Derived value', 'Reactive dependency'],
    'When would watch be a better fit?',
  ],
  [
    'mental-side-effect',
    'Side effect from Vue state?',
    'Use watch to trigger an action when reactive state changes.',
    ['Use watch', 'Change-triggered action', 'Side effect rather than value'],
    'What side effects should be debounced?',
  ],
  [
    'mental-child-data',
    'Data needed only by child?',
    'Start with props so the child’s dependency stays explicit.',
    ['Props first', 'Explicit dependency', 'Keep state narrow'],
    'When would provide/inject be reasonable?',
  ],
  [
    'mental-shared-data',
    'Data genuinely shared broadly?',
    'Consider shared state or a query cache, depending on whether it is client or server data.',
    ['Shared state or query cache', 'Check ownership', 'Use only for broad sharing'],
    'How would you distinguish server data from UI state?',
  ],
  [
    'mental-slow-ui',
    'UI is slow?',
    'Measure first, then optimize the actual user-visible bottleneck.',
    ['Measure first', 'Find bottleneck', 'Verify user impact'],
    'Which performance metric matches a delayed click?',
  ],
  [
    'mental-prod-bug',
    'Production bug?',
    'Trace the request and data path and gather evidence before assuming a cause.',
    ['Trace full path', 'Gather evidence', 'Avoid unsupported assumptions'],
    'What identifier helps connect frontend and backend logs?',
  ],
  [
    'mental-timeout',
    'Payment timed out?',
    'Unknown does not equal failed. Preserve an unknown or pending state and reconcile.',
    ['Unknown differs from failed', 'Represent pending state', 'Reconcile with authority'],
    'How would you prevent a duplicate retry?',
  ],
  [
    'mental-mutation-retry',
    'Retrying a mutation?',
    'Think about idempotency so repeated requests do not duplicate the intended effect.',
    ['Idempotency', 'Repeated mutation safety', 'Same intended effect'],
    'Where should idempotency be enforced?',
  ],
]

export const technicalFundamentalsCards: Omit<InterviewCard, 'followUpAnswer'>[] = flashcards.map(
  ([id, question, sampleResponse, keyBeats, followUp, codeExample]) => ({
    id: `technical-${id}`,
    category: 'technical-fundamentals',
    question,
    suggestedSeconds: 45,
    keyBeats,
    sampleResponse,
    followUp,
    ...(codeExample ? { codeExample } : {}),
  }),
)
